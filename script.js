// Script to handle form submission or other interactive elements
document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert("Message sent!");
});
